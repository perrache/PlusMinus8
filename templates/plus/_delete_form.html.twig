<form method="post" action="{{ path('app_plus_delete', {'id': plu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ plu.id) }}">
    <button class="btn">Delete</button>
</form>
