<form method="post" action="{{ path('app_saldo_delete', {'id': saldo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ saldo.id) }}">
    <button class="btn">Delete</button>
</form>
