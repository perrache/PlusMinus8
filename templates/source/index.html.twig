{% extends 'base.html.twig' %}

{% block title %}Source index{% endblock %}

{% block crud %}
    <h1>Source index</h1>
    <div class="new"><a href="{{ path('app_source_new') }}">Create new</a></div>

    <table class="table">
        <thead>
        <tr>
            <th>Id</th>
            <th>Name</th>
            <th>actions</th>
        </tr>
        </thead>
        <tbody>
        {% for source in sources %}
            <tr>
                <td>{{ source.id }}</td>
                <td>{{ source.name }}</td>
                <td>
                    <a href="{{ path('app_source_show', {'id': source.id}) }}">show</a>
                    <a href="{{ path('app_source_edit', {'id': source.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div class="new"><a href="{{ path('app_source_new') }}">Create new</a></div>
{% endblock %}
