{% extends 'base.html.twig' %}

{% block title %}Account{% endblock %}

{% block crud %}
    <h1>Account</h1>

    {{ Table|raw }}
    <tbody>
    <tr>
        <th>Id</th>
        <td>{{ account.id }}</td>
    </tr>
    <tr>
        <th>Name</th>
        <td>{{ account.name }}</td>
    </tr>
    <tr>
        <th>Bo</th>
        <td>{{ account.bo }}</td>
    </tr>
    <tr>
        <th>Lt</th>
        <td>{{ account.lt }}</td>
    </tr>
    <tr>
        <th>Import</th>
        <td>{{ account.import }}</td>
    </tr>
    </tbody>
    {{ closeTable|raw }}

    <a href="{{ path('app_account_index') }}">back to list</a>

    <a href="{{ path('app_account_edit', {'id': account.id}) }}">edit</a>

    <a href="{{ path('app_saldo_save', {'id': account.id}) }}">Saldo</a>

    {{ include('account/_delete_form.html.twig') }}
{% endblock %}
