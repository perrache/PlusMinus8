<form method="post" action="{{ path('app_minus_delete', {'id': minu.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ minu.id) }}">
    <button class="btn">Delete</button>
</form>
