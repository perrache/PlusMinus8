{# curTab #}
{# aElement #}

{% if curTab is defined and aElement is defined %}
    {% if app.current_route == 'route_sql' and curTab == app.request.get('tab') %}
        {% set extra = ' style="color: yellow"' %}
    {% else %}
        {% set extra = '' %}
    {% endif %}

    <div>
        <a href="{{ path('route_sql') }}/{{ curTab }}"{{ extra|raw }}>{{ '%02u - '|format(curTab) }}{{ aElement }}</a>
    </div>
{% endif %}
