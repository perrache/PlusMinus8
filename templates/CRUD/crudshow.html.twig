{% extends 'base.html.twig' %}

{% block crud %}
    <h1>{{ block('title') }}</h1>

    {% if records.(0) is defined %}
    <table>
        <tbody>
        {% for col in records.(0)|keys %}
            <tr>
                <th>{{ col|capitalize }}</th>
                <td>{{ records.(0).(col) }}</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    {% else %}
        <h3>no record found</h3>
    {% endif %}

    <div class="new"><a href="{{ path('app_' ~ block('tablename') ~ '_index') }}">back to list</a></div>
    <div class="new"><a href="{{ path('app_' ~ block('tablename') ~ '_edit', {'id': records.(0).id}) }}">edit</a></div>
    <div class="new"><a href="{{ path('app_' ~ block('tablename') ~ '_delete', {'id': records.(0).id}) }}">delete</a></div>
{% endblock %}
